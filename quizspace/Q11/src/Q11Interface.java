
public interface Q11Interface {
	
	//Method: factorial
	//Precondition: an valid index that is an -10 <= index < 11
	//Postcondition: if index < 0, return -1
	//Postcondition: if index >= 0, the value of the factorial 
	//		of that index. 
	//	(ex. the value at index 3 is 3! = 6).

	public int factorial(int index);

}
